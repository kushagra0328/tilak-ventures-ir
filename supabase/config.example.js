// Copy to supabase/config.js for local testing only.
// In production, inject these values through your hosting environment.
// The anon key is intended for browser use; NEVER put a Supabase service-role key here.
window.TILAK_SUPABASE = {
  url: 'https://YOUR-PROJECT.supabase.co',
  anonKey: 'YOUR-SUPABASE-ANON-KEY'
};
